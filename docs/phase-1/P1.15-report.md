# catboost-8z4.25 (P1.15) — Repoint the parity harness at the fork's NAMESPACE

**Date:** 2026-07-31 (`date` -> `Fri Jul 31 09:34:05 AM CEST 2026`)
**Repo:** `/home/dd/Gemini/catboost`, branch `phase-0`
**Result:** SUCCESS. All Section VI DoD items met.

---

## 1. Hardcoded path and source field before change (Step 1)

`tools/parity/parse_namespace.py` (pre-edit):

```
14: VENDOR_DIR = REPO_ROOT / "vendor" / "catboost"
15: NAMESPACE_PATH = VENDOR_DIR / "catboost" / "R-package" / "NAMESPACE"
...
43:     "source": "vendor/catboost/catboost/R-package/NAMESPACE",
```

This resolves to `vendor/catboost/catboost/R-package/NAMESPACE` — upstream's
copy, not the fork's own `NAMESPACE` at repo root (created by P1.0 /
catboost-8z4.19).

## 2. Change made (Step 2)

Edited `tools/parity/parse_namespace.py`:

- `NAMESPACE_PATH = VENDOR_DIR / "catboost" / "R-package" / "NAMESPACE"`
  -> `NAMESPACE_PATH = REPO_ROOT / "NAMESPACE"`
- `"source": "vendor/catboost/catboost/R-package/NAMESPACE"`
  -> `"source": "NAMESPACE"`
- Module docstring updated (`Parse upstream R-package/NAMESPACE...` ->
  `Parse the fork's NAMESPACE...`; `Reads:` line updated accordingly).

`VENDOR_DIR` and the `git -C $VENDOR_DIR rev-parse HEAD` call were left
untouched: they exist only to stamp `upstream_sha`/`upstream_tag`
provenance of the vendor pin the fork's NAMESPACE was originally copied
from — that is legitimate, unrelated provenance, not part of the "which
NAMESPACE do we read" defect the ticket targets. Not touched per the
surgical-scope guard.

**path_before:** `vendor/catboost/catboost/R-package/NAMESPACE`
**path_after:** `/home/dd/Gemini/catboost/NAMESPACE` (fork root)

## 3. Grep for other hardcoded vendor paths under tools/ (Step 3)

```
$ grep -rn "vendor/catboost\|VENDOR_DIR\|R-package" tools/
tools/vendor/acquire.sh:4:  # Re-runnable: if vendor/catboost already exists at the correct SHA, this
tools/vendor/acquire.sh:9:  #   dest_dir defaults to vendor/catboost (gitignored; not committed).
tools/vendor/acquire.sh:18: DEST_DIR="${1:-${REPO_ROOT}/vendor/catboost}"
tools/parity/run.sh:3:      # Requires vendor/catboost (tools/vendor/acquire.sh) and the CLI oracle binary
tools/parity/README.md:15-16: `vendor/catboost` (acquire it first via `tools/vendor/acquire.sh` if absent) and the fork's own `R-package/NAMESPACE`
tools/parity/parse_namespace.py:1,4,14,19,21,26,43: (fixed in this ticket)
tools/rename-package.py:5,11,47: (guards against, does not read from, vendor/catboost)
```

Classification of every hit not already fixed:

| file:line | verdict |
|---|---|
| `tools/vendor/acquire.sh:4,9,18` | correct-as-is — this script's whole job is cloning upstream *into* `vendor/catboost`; it describes upstream by design, not the fork's surface. |
| `tools/parity/run.sh:3` | correct-as-is — the comment says stage 1 "requires vendor/catboost"; stage 1 (`parse_namespace.py`) no longer reads NAMESPACE from there, but it still needs `vendor/catboost` to exist so it can `git rev-parse HEAD` for the `upstream_sha`/`upstream_tag` provenance fields. The requirement is real, just for a narrower reason than before. Not misleading enough to warrant a wording change under a "touch only parse_namespace.py unless another is equally one-line" boundary — flagged, not fixed. |
| `tools/parity/README.md:14-18` | already correct — it already documents stage 1 as reading "the fork's own `R-package/NAMESPACE`" (line 16) in addition to the vendor checkout for the SHA/tag. No change needed; the code now matches what the doc already claimed. |
| `tools/rename-package.py:5,11,47` | correct-as-is — line 47 is an explicit guard (`if "vendor/catboost" in str(root.resolve()): ...`) that *refuses* to operate inside vendor/catboost. It is the opposite of the defect class this ticket targets. |

No other tool hardcodes a stale vendor path describing the fork's R
surface. `parse_namespace.py` was the only offender.

## 4. Re-run harness, diff against fixture (Step 4)

```
$ cp tests/fixtures/parity/r_surface.json /tmp/.../r_surface_before.json
$ python3 tools/parity/parse_namespace.py
exports=19 s3methods=8 upstream_sha=b1bd2a6d77219e82a1acfcedfccb8e6f6c1ee084
$ diff r_surface_before.json tests/fixtures/parity/r_surface.json
2c2
< 	"source": "vendor/catboost/catboost/R-package/NAMESPACE",
---
> 	"source": "NAMESPACE",
```

**surface_diff:** the only line that differs is `source`. Every export
name, every S3 method, `export_count`, `s3method_count`, `upstream_tag`,
and `upstream_sha` are byte-identical before and after.

**Explanation:** the fork's root `NAMESPACE` (created verbatim by P1.0 /
catboost-8z4.19, which vendored the upstream R sources unmodified) and
upstream's `vendor/catboost/catboost/R-package/NAMESPACE` are still
byte-identical at this point in Phase 1 — confirmed directly:

```
$ diff NAMESPACE vendor/catboost/catboost/R-package/NAMESPACE
(no output — identical)
```

No package rename or capability addition has touched exports yet, so an
empty surface diff (apart from the intentional `source` provenance
change) is exactly the expected outcome, not evidence the tool is reading
something unexpected.

## 5. Export count verified independently (Step 5)

```
$ grep -c '^export(' NAMESPACE
19
$ grep -c '^S3method(' NAMESPACE
8
```

Matches the tool's `exports=19 s3methods=8` exactly, counted independently
of `parse_namespace.py`'s own regex logic.

## 6. Guard re-check (Step 6)

| Guard | Status |
|---|---|
| Evidence — every claim quotes exact command output or file:line | Met (§1-5 above) |
| Anti-pivot — no workaround shift | N/A, no blocker hit |
| Boundary — touch only paths named in Section IV | Met: only `tools/parity/parse_namespace.py` edited; `tests/fixtures/parity/r_surface.json` regenerated by running the (now-fixed) tool, not hand-edited |
| Read-only pin — zero writes under `vendor/catboost/` | Met, verified below |
| Tone | Met |

```
$ git -C vendor/catboost status --porcelain
(empty)
```

Verified empty both at task start and at task finish.

## Files changed

- `tools/parity/parse_namespace.py` — repointed `NAMESPACE_PATH` at fork
  root, updated `source` field and docstring.
- `tests/fixtures/parity/r_surface.json` — regenerated (only `source`
  field differs).

## Suggested commit message

```
fix(tools): read fork's NAMESPACE in parse_namespace.py, not upstream's

Spec 4.5 defines the R surface as every export() in the fork's NAMESPACE.
parse_namespace.py hardcoded the vendor copy instead, which fails silently
today (the two files are identical) but would compute Phase 2 parity rows
against the wrong surface the moment the fork's NAMESPACE first diverges.
```

---

## Output (Section V schema)

```yaml
task_id: P1.15
success: true
data:
  path_before: "vendor/catboost/catboost/R-package/NAMESPACE"
  path_after: "/home/dd/Gemini/catboost/NAMESPACE"
  other_stale_paths:
    - file: "tools/vendor/acquire.sh"
      line: "4,9,18"
      verdict: "correct-as-is (describes upstream by design, it's the cloner)"
    - file: "tools/parity/run.sh"
      line: 3
      verdict: "correct-as-is (still needs vendor/catboost for upstream_sha provenance; comment slightly imprecise but not misleading enough to fix under one-line-only boundary)"
    - file: "tools/parity/README.md"
      line: "14-18"
      verdict: "correct-as-is (already documents fork NAMESPACE as the read target)"
    - file: "tools/rename-package.py"
      line: 47
      verdict: "correct-as-is (guards against, does not read from, vendor/catboost)"
  surface_diff:
    - "source: \"vendor/catboost/catboost/R-package/NAMESPACE\" -> \"NAMESPACE\" (only diff; fork and vendor NAMESPACE are still byte-identical, verified via direct diff)"
  export_count: 19
report_path: docs/phase-1/P1.15-report.md
vendor_clean: true
error_log: null
```
