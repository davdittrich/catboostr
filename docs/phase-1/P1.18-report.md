# catboost-8z4.28 (P1.18) — Fifth package-name spelling in the caret model spec

**Date:** 2026-07-31
**Repo:** `/home/dd/Gemini/catboost`, branch `phase-0`
**Result:** SUCCESS. All Section VI DoD items met. No sixth spelling found.

---

## 1. Verify the single occurrence (Step 1)

```
$ grep -rF 'library = "catboost"' R/ man/ tests/
R/catboost.caret.R:                       library = "catboostr",
```

(shown post-fix; content pre-fix, confirmed first):

```
R/catboost.caret.R:6:                       library = "catboost",
```

Exactly one hit, matching the ticket's reference data. Confidence: 100 (exact grep output).

## 2. Fifth pattern added to `tools/rename-package.py` (Step 2)

Added as the 5th entry in `SUBS`, following the existing style (label, compiled pattern, replacement). Docstring counts ("four" -> "five") updated in the same file for consistency; no other pattern touched.

```diff
     ("package_arg", re.compile(r'(package\s*=\s*)"catboost"'), r'\1"catboostr"'),
+    ("caret_library", re.compile(r'(library\s*=\s*)"catboost"'), r'\1"catboostr"'),
 ]
```

Confidence: 100 (exact diff shown).

## 3. Re-run over the fork tree (Step 3)

```
$ python3 tools/rename-package.py .
library: 0
colons: 0
test_check: 0
package_arg: 0
caret_library: 1
files changed: 1
  R/catboost.caret.R
```

Substitutions per pattern: `library=0, colons=0, test_check=0, package_arg=0, caret_library=1`. Only the new pattern fired — the four prior patterns are correctly idempotent against the already-renamed tree. Confidence: 100.

Result in `R/catboost.caret.R:6`:

```
                       library = "catboostr",
```

## 4. Minimality re-proof (Step 4)

Fresh copy of the pinned upstream tree, amended script run against it, diff against the committed tree.

```
$ cp -r vendor/catboost/catboost/R-package/. $SCRATCH/fresh/
$ python3 tools/rename-package.py $SCRATCH/fresh
library: 1
colons: 2
test_check: 1
package_arg: 31
caret_library: 1
files changed: 9
  R/catboost.R
  R/catboost.caret.R
  man/catboost.load_pool.Rd
  man/catboost.train.Rd
  tests/testthat/test_caret_parameter_tuning.R
  tests/testthat/test_model.R
  tests/testthat/test_on_trimmed_adult_dataset.R
  tests/testthat/test_pool.R
  tests/testthat.R

$ for t in R man tests/testthat tests/testthat.R NAMESPACE; do
    diff -rq "$SCRATCH/fresh/$t" "$t"
  done
$ echo "diff exit:$?"
diff exit:0
```

Empty diff across all five `TARGETS` entries. **minimality_diff_empty: true.** Confidence: 100 (command output quoted verbatim).

## 5. Enumeration of every bare `catboost` occurrence in R/, man/, tests/ (Step 5)

```
$ grep -rn 'catboost' R/ man/ tests/ | wc -l
994 lines / 1029 occurrences (a line can hold >1 hit, e.g. `catboostr::catboost.caret`)
```

Classification (occurrence-level, computed with a small script splitting `file:line:content` before pattern-matching — an earlier line-level attempt double-counted because `grep -v` also matched against the `catboost.caret.R` *filename* prefix, which was discarded in favor of this corrected count):

| Class | Count | What it covers |
|---|---|---|
| (a) package identity | 37 | `library(catboostr)`, `catboostr::`, `test_check("catboostr")`, `package = "catboostr"`, `library = "catboostr"` (incl. the fix in §3), `@useDynLib libcatboostr`. All already read `catboostr` — zero unrenamed identity hits remain (verified: `grep -rnE '"catboost"'`, `library\s*=\s*"catboost"`, `package\s*=\s*"catboost"`, `catboost::`, `library\(catboost\)` all return empty against R/, man/, tests/). |
| (b) function-name prefix | 942 | `catboost.train`, `catboost.predict`, `catboost.caret`, `catboost.Pool`, etc. — upstream's `catboost.<verb>` S3-style export naming, correctly left unchanged (it is not the R package name). |
| (c) prose / other identifiers | 50 | `\url{https://catboost.ai/docs/...}` doc links (11), `message("Training catboost with default parameters!...")` user-facing text (2), "For catboost binary format" doc prose (2), the CatBoost engine's hardcoded working directory `'catboost_info'` (4, unrelated to the R package name), `R/install.R`'s fork-added dynlib installer (8: function name `catboost_download_dynlib`, the upstream GitHub release URL `github.com/catboost/catboost`, and `libcatboostr-*` artifact filenames — this file doesn't exist upstream, so the rename script's `files()` walk covers it but none of the 5 patterns match its content), JSON test fixtures under `tests/fixtures/{oracle,oracle-cli,parity}/*.json` recording upstream's own version/CLI metadata (`"catboost_version": "1.2.10"`, `catboost fit`/`catboost calc` CLI invocations, asset URLs) (18), and local R variable names `catboost_model`/`catboost_info` in test bodies (5). |

`37 + 942 + 50 = 1029`. Confidence: 95 (script-derived counts, spot-checked against the raw line dump; residual risk is in edge-case double counting within a single line, mitigated by counting matched substrings rather than lines).

**Sixth spelling found: none.** Every occurrence of the bare string `catboost` in R/, man/, tests/ is either (a) an already-renamed identity reference, (b) the untouched function-name prefix, or (c) prose/external-reference/unrelated-identifier text that does not participate in R package name resolution. Confirmed by exhaustive negative grep for every un-renamed identity form (`"catboost"` exact, `library(catboost)`, `catboost::`, `package = "catboost"`, `library = "catboost"`) returning empty.

`DESCRIPTION:1` (`Package: catboostr`) and `NAMESPACE:36` (`useDynLib(libcatboostr, ...)`) were spot-checked out of curiosity (not in the ticket's R/man/tests scope) and are already correctly renamed — not part of this ticket's diff.

## 6. `catboost.caret$library` and the caret test (Step 6)

`R/catboost.caret.R:6` now reads `library = "catboostr"`. caret's `train()` passes this field to `checkInstall`, which resolves the R package to load — it now names the fork's actual package (`catboostr`, per `DESCRIPTION:1` and the vendored/renamed sources), not the removed upstream `catboost`. `tests/testthat/test_caret_parameter_tuning.R` will therefore no longer fail with "package not found" for this reason. Static reasoning only — the suite cannot execute until `catboostr` is built and installed (P1.8 scope, `catboost-8z4.16`), so this is not a runtime-verified claim. Confidence: 90 (mechanism traced in `catboost.caret.R`, not executed).

## 7. Guard re-check (Step 7)

```
$ git -C vendor/catboost status --porcelain
(empty)
```

Zero writes under `vendor/catboost/`. Confidence: 100.

```
$ git status --porcelain
 M R/catboost.caret.R
 M tools/rename-package.py
```

Only the two files named in the ticket's Section IV scope changed. No hand edits — the `R/catboost.caret.R` change is entirely a product of re-running the amended `tools/rename-package.py` (§3). No writing git command was run; the diff is reported for the controller to commit.

## 8. Section V Output Schema

```yaml
task_id: P1.18
success: true
data:
  occurrences_fixed: 1
  substitutions_per_pattern:
    library: 0
    colons: 0
    test_check: 0
    package_arg: 0
    caret_library: 1
  minimality_diff_empty: true
  bare_catboost_classified:
    identity: 37
    function_prefix: 942
    prose: 50
  sixth_spelling_found: []
report_path: docs/phase-1/P1.18-report.md
vendor_clean: true
error_log: null
```
