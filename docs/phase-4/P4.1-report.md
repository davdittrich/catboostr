# catboost-8z4.50 (P4.1) — `catboost.calc_feature_statistics` parity

Task 1 of `docs/superpowers/plans/2026-08-03-phase4-analysis-parity.md`, executed per the
ticket's hermetic spec.

## Step 1 — Python oracle introspection

```
$ uv run --frozen --project tools/oracle python -c "help(CatBoost.calc_feature_statistics)"
```

Signature: `calc_feature_statistics(self, data, target=None, feature=None, prediction_type=None,
cat_feature_values=None, plot=True, max_cat_features_on_plot=10, thread_count=-1,
plot_file=None)`. Source (`catboost/core.py`) shows the real computation is entirely delegated
to `self._object._get_binarized_statistics` (`_catboost.pyx:6057`), which calls vendor C++
`NCB::GetBinarizedStatistics` (`catboost/private/libs/quantized_pool_analysis/
quantized_pool_analysis.h`) — an existing, self-contained vendor library function, confirmed by
reading `quantized_pool_analysis.cpp` in full (`GetBinarizedStatistics`, `GetCatFeaturePerfectHash`,
`GetFeatureTypeAndInternalIndex`, `GetCatFeatureValues`). Confidence 95 (exact vendor source read).

## Step 2 — R signature design / native-glue gap check

`grep -n "GetBinarizedStatistics\|GetFeatureTypeAndInternalIndex\|Border\b" src/catboostr.cpp`
found no existing entry point exposing per-feature binning/statistics data — new C++ glue
required. `catboost.eval_metrics` (R/catboost.R:3387) confirmed the `(model, pool, ...)`
calling convention. New R signature (append-only, no existing signature touched):

```r
catboost.calc_feature_statistics(model, pool, feature = NULL, prediction_type = NULL,
                                  cat_feature_values = NULL, thread_count = -1)
```

`plot`/`max_cat_features_on_plot`/`plot_file` are dropped: spec Sec 4.3's Structural row scopes
this ticket to the statistics tensor only, never rendering, and adding a plotting dependency
(no `DiagrammeR`/graphviz-class binding exists in `R/` today, confirmed 2026-08-03) would be
unrequested scope. `target` is dropped too: `pool` already carries the label via the same
`catboost.pool.get_label`-backed mechanism `catboost.eval_metrics` relies on — no separate
target plumbing needed.

## Step 3 — Implementation

**Native glue** (`src/catboostr.cpp`, `.h`, `init.c`, `src/CMakeLists.txt`): four thin wrappers
around the vendor `NCB::` functions, linked via a new `private-libs-quantized_pool_analysis`
target dependency (same target Python's own `_catboost.pyx` links against):
- `CatBoostGetBinarizedStatistics_R` — wraps `GetBinarizedStatistics`, returns a list of
  7-field stat lists (borders/binarized_feature/mean_target/mean_weighted_target/
  mean_prediction/objects_per_bin/predictions_on_varying_feature), cat features first then
  float features (matching the vendor function's own ordering, verified by reading
  `quantized_pool_analysis.cpp:582-618`).
- `CatBoostGetFeatureTypeAndInternalIndex_R` — wraps `GetFeatureTypeAndInternalIndex`.
- `CatBoostCalcCatFeaturePerfectHash_R` — wraps `GetCatFeaturePerfectHash` (returns as
  `REALSXP`, not `INTSXP`: the underlying `ui32` can exceed R's 32-bit signed integer range,
  same convention `CatBoostHashStrings_R` already uses for hash values).
- `CatBoostGetCatFeatureValues_R` — wraps `GetCatFeatureValues`.

All feature-resolution/prediction_type-defaulting/cat-value-to-hash-ordering orchestration
logic lives in R (`R/catboost.R catboost.calc_feature_statistics`), a direct line-by-line port
of `catboost.core.CatBoost.calc_feature_statistics`'s algorithm, calling only these 4 primitives
— matching this fork's established pattern (business logic in R, vendor computation in C++).

**Known, documented R-Pool-architecture limitation** (found while smoke-testing against the
oracle, confidence 95 — read `R/catboost.R:222,410` `CatBoostHashStrings_R` call sites):
`catboost.load_pool()`/`catboost.from_matrix()` pre-hash categorical columns into floats
client-side before the data ever reaches the native `TDataProvider`, unlike Python's `Pool`
(which retains the original strings server-side). So `NCB::GetCatFeatureValues` has nothing to
recover from an R-built Pool's categorical column — `cat_values` defaults to empty unless the
caller supplies `cat_feature_values` explicitly (the same escape-hatch parameter Python's own
docstring documents for exactly this situation). This is not a P4.1 bug: reworking Pool
construction to retain original categorical strings is a Pool-architecture change, out of this
ticket's `Boundary` guard (must not touch `plot_tree`/`catboost.compare`/other P4.x rows or
Pool internals). Documented in the `@param cat_feature_values` roxygen block and the test file's
header comment.

## Step 4 — Oracle fixture + differential test

`tools/oracle/gen_calc_feature_statistics_fixture.py`: trains a small `CatBoostClassifier`
(`Logloss`, `one_hot_max_size=4` — required, `GetBinarizedStatistics` only supports one-hot
categorical features, confirmed by the vendor `CB_ENSURE` message hit while first running the
generator unconfigured) on 2 float + 1 categorical feature, records
`calc_feature_statistics(feature=None)` (all features), a single float feature with an explicit
`prediction_type`, and a single categorical feature — covering both the `borders` and
`cat_values` statistics branches.

`tests/testthat/test_calc_feature_statistics.R`: canonical field-by-field comparison against
the fixture (`tolerance = 1e-6`; `cat_values` compared exactly as strings), plus a rendering/
class smoke test (`expect_type(result, "list")`) and two error-path assertions (unknown
`prediction_type`, unknown feature name).

```
$ Rscript -e 'library(testthat); library(catboostr); test_file("tests/testthat/test_calc_feature_statistics.R", reporter = "summary")'
Welcome back dd!
Working directory is: /home/dd/Gemini/catboost/.worktrees/phase-4-analysis-parity
calc_feature_statistics:
test_calc_feature_statistics.R: ...........................................

══ DONE ════════════════════════════════════════════════════════════════════════
```
45/45 assertions pass, 0 failures.

## Step 5 — Parity matrix

`tests/fixtures/parity/matrix.dispositioned.json`: `CatBoost.calc_feature_statistics`,
`CatBoostClassifier.calc_feature_statistics`, `CatBoostRegressor.calc_feature_statistics`,
`CatBoostRanker.calc_feature_statistics` flipped `red` → `green`, `test_id` set to
`tests/testthat/test_calc_feature_statistics.R` on each (minimal targeted edit — 8 lines
changed, verified via `git diff --stat` and `jq` re-parse — a naive whole-file `jq` rewrite was
rejected mid-task because it silently reformatted every `1e-12` tolerance literal to `1E-12`
across unrelated rows; reverted and redone as 4 exact-context `Edit` calls instead).

## Step 6 — Build

Environment note: this worktree (`.worktrees/phase-4-analysis-parity`) has no local
`vendor/catboost` checkout (gitignored, per-worktree, ~1.3 GiB — not copied when the worktree
was created). Built against the main checkout's existing pinned snapshot via
`CATBOOSTR_VENDOR_SRC=/home/dd/Gemini/catboost/vendor/catboost` /
`CATBOOSTR_THIRDPARTY_SRC=/home/dd/Gemini/catboost/vendor/thirdparty` (the `configure` script's
own documented override point), `CATBOOSTR_PYTHON3`/`CATBOOSTR_CYTHON` pointed at a local
`cython`-equipped venv (`/tmp/cyvenv`, no system `cython` on `PATH`). Vendor pin never written:

```
$ git -C /home/dd/Gemini/catboost/vendor/catboost status --porcelain
(no output, exit 0)          # verified before AND after the full build/test run
```

```
$ CATBOOSTR_VENDOR_SRC=... CATBOOSTR_THIRDPARTY_SRC=... CATBOOSTR_PYTHON3=... CATBOOSTR_CYTHON=... \
  R CMD INSTALL --preclean .
[...]
[100%] Built target catboostr
*** installed .../libcatboostr.so -> inst/libs/libcatboostr.so
** libs
** R
** inst
** byte-compile and prepare package for lazy loading
** help
*** installing help indices
** building package indices
** testing if installed package can be loaded from temporary location
** checking absolute paths in shared objects and dynamic libraries
** testing if installed package can be loaded from final location
** testing if installed package keeps a record of temporary installation path
* DONE (catboostr)
```

Exit code 0.

## Definition-of-Done checklist

- [x] `catboost.calc_feature_statistics` exists, S3-dispatch-free, covers all 4 matrix rows
      (R has one model class; upstream's 4 Python classes all route through the same bound
      method, matched by the single new R function).
- [x] Structural differential test passes against the pinned Python oracle; 4 matrix rows
      flipped green.
- [x] `git -C vendor/catboost status --porcelain` empty before and after (verified against the
      actual vendor snapshot used, `/home/dd/Gemini/catboost/vendor/catboost`, since this
      worktree has no local copy of its own).

## Files changed

- `R/catboost.R` — new `catboost.calc_feature_statistics` (append-only; no existing signature
  touched).
- `src/catboostr.cpp`, `src/catboostr.h`, `src/init.c` — 4 new native entry points.
- `src/CMakeLists.txt` — link `private-libs-quantized_pool_analysis`.
- `NAMESPACE`, `man/catboost.calc_feature_statistics.Rd` — regenerated via
  `roxygen2::roxygenise(".", load_code = roxygen2::load_source)` (source-only load, avoids
  triggering a DLL compile from inside `roxygenise()` itself).
- `tools/oracle/gen_calc_feature_statistics_fixture.py`,
  `tests/fixtures/oracle/calc_feature_statistics.json` — new oracle fixture.
- `tests/testthat/test_calc_feature_statistics.R` — new differential test.
- `tests/fixtures/parity/matrix.dispositioned.json` — 4 rows flipped green.

## Output (per ticket's Section V schema)

```toon
task_id: catboost-8z4.P4.1
success: true
data:
  function_implemented: true
  matrix_rows_green: 4
  matrix_rows_total: 4
  new_native_entry_point: true
  report_path: docs/phase-4/P4.1-report.md
  vendor_clean: true
  error_log: null
```
