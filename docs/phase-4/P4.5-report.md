# catboost-8z4.54 (P4.5) — `get_object_importance` MultiClass error-path parity (#869)

Task: verify `catboost.get_object_importance` (`R/catboost.R:3219`) fails
identically to Python on a `MultiClass` model (upstream #869), per
`docs/phase-2/P2.4-report.md` Finding 1 (confidence 95). No core (`vendor/catboost/`)
writes permitted.

## Step 1 — Python's exact behavior (from P2.4-report.md Finding 1, quoted)

```
fit ok, classes_: [0 1 2]
get_object_importance FAILED: CatBoostError catboost/private/libs/documents_importance/ders_helpers.cpp:95: Error function MultiClass is not supported yet in ostr mode
```

Root cause (P2.4, confidence 95): `ders_helpers.cpp` `GetEvaluateDerivativesFunc`
is a hardcoded `switch` over `ELossFunction` with no `MultiClass` case; falls to
`default: CB_ENSURE(false, "Error function " + ToString(lossFunction) + " is not
supported yet in ostr mode")` at line 95. Structural (exhaustive allowlist), not
a version-specific regression.

## Step 2 — R-side entry-point trace

- `R/catboost.R:3219` `catboost.get_object_importance` — argument validation only
  (`inherits(pool, "catboost.Pool")`, handle-null checks, `top_size` range), then
  `.Call("CatBoostEvaluateObjectImportances_R", ...)`. No try/catch here.
- `src/catboostr.cpp:1934` `CatBoostEvaluateObjectImportances_R` — calls
  `GetDocumentImportances(...)` (the vendor entry point that reaches
  `ders_helpers.cpp:95`) inside the `R_API_BEGIN()`/`R_API_END()` pair
  (`src/catboostr.cpp:74-89`):
  ```cpp
  #define R_API_BEGIN() ... try {
  #define R_API_END() \
      } catch (std::exception& e) { \
          error(e.what()); \
      } \
      RestoreOriginalLogger();
  ```
  `CB_ENSURE` throws `TCatBoostException` (derives from `std::exception`); its
  `what()` is exactly the formatted `"<file>:<line>: <message>"` string. R's
  `error(e.what())` raises a standard R condition (`simpleError`/`error`/
  `condition`) whose `conditionMessage()` is that string verbatim — no
  truncation, no re-wording, no ticket/internal-ID leakage.

Confidence: 100 (direct code quotes, both files read in full at the relevant
sections).

## Step 3 — Reproduce current R behavior

Repro script (fit `MultiClass` on synthetic 3-class data, then call
`catboost.get_object_importance`):

```r
model <- catboost.train(pool, NULL, list(iterations = 10, loss_function = "MultiClass", ...))
res <- tryCatch(catboost.get_object_importance(model, pool, pool, top_size = 3),
                error = function(e) e)
```

Verbatim output (`Rscript` run against the pre-existing installed `catboostr`,
no code touched yet):

```
fit ok
CLASS:  simpleError,error,condition
MESSAGE:  catboost/private/libs/documents_importance/ders_helpers.cpp:95: Error function MultiClass is not supported yet in ostr mode
```

**R's message is byte-for-byte identical to Python's** (`catboost/private/libs/
documents_importance/ders_helpers.cpp:95: Error function MultiClass is not
supported yet in ostr mode`), including the `.cpp:95` line number. Confirms the
`R_API_BEGIN`/`R_API_END` trace in Step 2: the C++ exception message passes
through unmodified.

## Step 4 — Gap analysis

None. R's `simpleError`/`conditionMessage()` is the idiomatic R analog of
Python's `CatBoostError` (both are "the language's standard exception
mechanism, raised from the same core `CB_ENSURE` call site, carrying the same
message"). There is no R-side wrapping, truncation, or generic fallback message
to fix — `error(e.what())` already forwards the native message unchanged.
**No code change made** (R or C++); Constraint III's "no core patch" and
"fix only R-side error surfacing... never the C++ switch/check" are moot since
nothing needed fixing.

Confidence: 95 (matches P2.4 Finding 1's own confidence — same evidence
chain, one additional hop through the R glue layer which is now also directly
observed, not inferred).

## Step 5 — Differential test

`tests/testthat/test_object_importance_multiclass.R` (new): trains a
`MultiClass` model in R, asserts `catboost.get_object_importance(...)` raises
an error whose message equals (fixed-string match) the exact P2.4-quoted
Python message. This is a regression guard, not new error-translation code —
if a future R or vendor change ever alters or generalizes this message, the
test fails and flags the drift.

## Step 6 — Test run (no rebuild needed — zero source changes)

```
$ Rscript -e 'library(catboostr); testthat::test_file("tests/testthat/test_object_importance_multiclass.R")'
[ FAIL 0 | WARN 0 | SKIP 0 | PASS 1 ]
```

## Step 7 — Guard re-check

- `git -C /home/dd/Gemini/catboost/vendor/catboost status --porcelain` — empty.
- No writes anywhere under `vendor/catboost/`.
- No `src/catboostr.cpp` or `R/catboost.R` changes — behavior already matched
  Python before this task; only a test file was added.
- Scope: MultiClass only, `get_object_importance` only. Finding 2
  (MultiRMSE/MultiLogloss) and P4.4 (`get_feature_importance`) untouched.
- `tests/fixtures/parity/matrix.dispositioned.json`: no `get_object_importance`
  row present in the matrix (checked — no `inventory_row_id` matches
  `object_importance`); nothing to flip.

## Definition of Done

- [x] R raises the same error message as Python on `get_object_importance`
      MultiClass (`ders_helpers.cpp:95` text, verbatim) — already true before
      this task; verified, not implemented.
- [x] Error-path differential test passes (1/1).
- [x] `git -C vendor/catboost status --porcelain` empty.
- [x] Report written with exact command output, referencing P2.4 Finding 1.

## Files changed

- `tests/testthat/test_object_importance_multiclass.R` — new differential
  error-path test (only change in this task).

## Result summary

```yaml
task_id: catboost-8z4.54
success: true
data:
  r_error_matched_python_before_change: true
  code_changed: false
  error_path_test_passing: true
report_path: docs/phase-4/P4.5-report.md
vendor_clean: true
error_log: null
```
